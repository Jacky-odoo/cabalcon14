<odoo>
    <data>
        <!-- Agrego el Check en la pestaña de factura del partner -->
        <record id="view_partner_property_form_pe" model="ir.ui.view">
            <field name="name">res.partner.property.form.inherit.pe</field>
            <field name="model">res.partner</field>
            <field name="priority">10</field>
            <field name="inherit_id" ref="account.view_partner_property_form"/>
            <field name="arch" type="xml">
                <field name="trust" position="after">
                    <field name="is_gruped"/>
                </field>
            </field>
        </record>
    </data>
</odoo>